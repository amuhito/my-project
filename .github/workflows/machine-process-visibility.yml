name: Machine Process Visibility PoC

on:
  pull_request:
    paths:
      - ".github/workflows/machine-process-visibility.yml"
      - "Makefile"
      - "docs/development.md"
      - "apps/machine-process-visibility/**"
  push:
    branches:
      - main
      - "codex/**"
    paths:
      - ".github/workflows/machine-process-visibility.yml"
      - "Makefile"
      - "docs/development.md"
      - "apps/machine-process-visibility/**"

jobs:
  check:
    name: Test and build
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: apps/machine-process-visibility/backend/requirements.txt

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: apps/machine-process-visibility/package-lock.json

      - name: Install backend dependencies
        run: make -C apps/machine-process-visibility setup-backend

      - name: Install frontend dependencies
        working-directory: apps/machine-process-visibility
        run: npm ci

      - name: Run validation
        run: make machine-check
